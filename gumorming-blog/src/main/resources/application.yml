server:
  port: ${BLOG_PORT:7777}

spring:
  application:
    name: ${APP_NAME:GuMorming's Blog}
  datasource:
    driver-class-name: ${jdbc.driver}
    url: ${jdbc.url}
    username: ${jdbc.username}
    password: ${jdbc.password}
  # 文件上传配置
  servlet:
    multipart:
      max-file-size: 2MB
      max-request-size: 5MB
  main:
    # 允许循环依赖
    allow-circular-references: false

mybatis-plus:
  configuration:
    # 日志
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config:
    db-config:
      # 逻辑删除字段名
      logic-delete-field: delFlag
      logic-delete-value: 1
      logic-not-delete-value: 0
      id-type: auto

oss:
  accessKey: Y5b7bE9qs_lljIu2xcm6NZYiVWenxpQL1m8_NbBf
  secretKey: pu14hY22gnXwY8xQ5K1JneXgqijOJKmcANN2MYzT
  bucket: gumorming-blog
  cdnPath: http://rxgoapsou.hd-bkt.clouddn.com/